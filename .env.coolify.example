# ===========================================
# COOLIFY ENVIRONMENT VARIABLES
# ===========================================
# Copy these variables to your Coolify service configuration
# Generate secure secrets using: openssl rand -hex 32

# ===========================================
# REQUIRED VARIABLES
# ===========================================

# Database connection (if using external database)
# For Coolify-managed PostgreSQL, this is auto-configured
DATABASE_URL=postgres://medusa:your_password@postgres:5432/medusa

# Redis connection (if using external Redis)
# For Coolify-managed Redis, this is auto-configured
REDIS_URL=redis://redis:6379

# Security secrets (MUST CHANGE IN PRODUCTION)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
COOKIE_SECRET=your-super-secret-cookie-key-min-32-chars

# ===========================================
# CORS CONFIGURATION
# ===========================================
# Your storefront URL(s) - comma separated
STORE_CORS=https://your-storefront.com,https://www.your-storefront.com

# Your admin URL(s) - typically same as BACKEND_URL
ADMIN_CORS=https://your-api.com

# Auth CORS - typically includes both storefront and admin
AUTH_CORS=https://your-storefront.com,https://your-api.com

# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=production
PORT=9000

# Public URL of your backend API
BACKEND_URL=https://your-api.com

# Worker mode: 'server' (combined) | 'worker' (separate worker) | 'shared' (default)
WORKER_MODE=server

# Disable admin panel (set to true for separate admin deployment)
DISABLE_ADMIN=false

# ===========================================
# OPTIONAL: MINIO/S3 STORAGE
# ===========================================
# Uncomment and configure if using MinIO or S3 for file storage

# MINIO_ENDPOINT=https://your-minio.com
# MINIO_ACCESS_KEY=your_access_key
# MINIO_SECRET_KEY=your_secret_key
# MINIO_BUCKET=medusa-uploads
# MINIO_USE_SSL=true

# ===========================================
# OPTIONAL: EMAIL CONFIGURATION
# ===========================================
# Uncomment and configure for email functionality

# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASSWORD=your_email_password
# SMTP_FROM=noreply@your-domain.com
